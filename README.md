# DrWebTest

Приложение для Android, позволяющее просматривать список установленных приложений на устройстве, изучать их детальную техническую информацию и вычислять контрольную сумму (SHA-256) APK-файлов.

## 📹 Демонстрация работы

![AppDemo](./demo.mp4)

## ✨ Особенности

* **Список приложений:** Отображение всех установленных на устройстве приложений с их иконками, названиями и версиями.
* **Детальная информация:** Просмотр расширенных данных о приложении:
    * Версия и код версии.
    * Целевой (Target) и минимальный (Min) SDK.
    * Пути к APK и данным.
    * Размер APK и даты установки/обновления.
    * Список запрашиваемых разрешений (Permissions).
* **Вычисление хеша:** Расчет SHA-256 хеш-суммы APK-файла.
    * Реализовано кэширование хешей в локальной базе данных для ускорения повторного доступа.
    * Автоматическое обновление хеша, если приложение было обновлено (сверка по `lastUpdateTime`).
* **Запуск приложений:** Возможность запустить выбранное приложение прямо из экрана деталей.
* **Современный UI:** Интерфейс полностью построен на **Jetpack Compose** с использованием Material Design 3.

## 🛠 Технологический стек

Приложение написано на **Kotlin** с использованием современных библиотек и подходов:

* **UI:** Jetpack Compose (Material 3).
* **Архитектура:** Clean Architecture (Layers: UI, Domain, Data) + MVVM.
* **Внедрение зависимостей (DI):** Koin.
* **Асинхронность:** Kotlin Coroutines & Flow.
* **База данных:** Room (для кэширования хеш-сумм).
* **Загрузка изображений:** Coil 3(для загрузки иконок приложений).
* **Навигация:** Navigation Compose (Type-safe navigation).

## 📂 Структура проекта

Проект разделен на слои для лучшей масштабируемости и тестируемости:

* `ui` — Компоненты интерфейса (Screens, ViewModels, Theme).
* `domain` — Бизнес-логика (UseCases, например `ApkHashUseCase`).
* `data` — Работа с данными (Репозитории, Room DAO, модели).
* `database` — Конфигурация Room.

